services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data

  web:
    build: ./frontend
    ports:
      - "8080:80"
    depends_on:
      - api
